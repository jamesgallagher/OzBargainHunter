X8: add the four missing test groups the card requires

- worker: real child process exits 0 on SIGTERM with the DB closed cleanly
- /healthz: both states (healthy 200 within 3 intervals, unhealthy 503 after)
- mute: removes the rule rows from pending_alerts (X8)
- render: one render test per screen - all nine screens (7.1)

Also: add the test:web npm script (X8), fix the architecture-guard
forbidden path strings in comments, reorder csrf/gate signatures to match
the tests, fix import depths, and mirror the verified email on the
response header (NextResponse.next() does not propagate request headers).

Gate: npm test 252/252, npm run lint 0 errors, next build exit 0 (19 routes).
